# frontend/Dockerfile
FROM node:18-alpine

WORKDIR /app

# Copy package manifests & install
COPY package.json package-lock.json ./
RUN npm ci

# Copy rest of your React code
COPY . .

# Expose Vite’s dev server & start it
EXPOSE 3000
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
